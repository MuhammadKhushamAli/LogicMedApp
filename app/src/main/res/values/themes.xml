<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LogicMed" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="button_color">@color/black</item>
        <item name="button_text_color">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="bottom_nav_color">@color/light_nav</item>
    </style>

    <style name="Theme.LogicMed" parent="Base.Theme.LogicMed" />
</resources>